{
  "name": "myhome",
  "version": "1.0.0",
  "private": true,
  "description": "Мой Дом — платформа управления жилым домом (ваад-байт). Android-обёртка вокруг прототипа.",
  "scripts": {
    "build": "node tools/extract-prod.mjs && npx cap sync android",
    "extract": "node tools/extract-prod.mjs",
    "fonts": "node tools/fetch-fonts.mjs",
    "apk:debug": "npm run build && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run build && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  }
}