# РОЛИ И ПРАВА
Собрано из живого прототипа: 24.08.2026, 17:55:34 · сборка 24.08-547 · My Home — интерактивный прототип

Картинка — «схемы/схема-2-роли-шляпы.svg».

## Как устроено (вербатим из кода)
```
Поверх надеваются шляпы: ВААД (u.hatVaad) и СПЕЦИАЛИСТ (мастер с тем же
   телефоном — specByPhone). Записи «role» у людей больше НЕТ: тип записи
   один — человек-жилец; председательство — флаг-шляпа, его надевают и снимают
   (передача дома), личность и история при этом не меняются.
   Выход из аккаунта один — в жильце (в базовой личности).
   ========================================================================= */
```

## Роли комитета
`const VROLES = [['chair','pr_r_chair'],['treas','pr_r_treas'],['secr','pr_r_secr'],['zavh','pr_r_zavh'],['memb','pr_r_memb']];`

## Ключевые решения
- Вход всегда как жилец; полоска режимов рисуется сама при двух и более шляпах.
- Шляпу ваада нельзя надеть самому — только действующий ваад назначает.
- «Стать специалистом» — кнопка из окна жильца, без новой регистрации.
- До двух взрослых на квартиру; третий — предупреждение и замена по решению ваада.