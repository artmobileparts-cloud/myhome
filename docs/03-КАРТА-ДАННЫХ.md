# КАРТА ДАННЫХ (вербатим из кода — единый источник)
Собрано из живого прототипа: 23.08.2026, 20:26:03 · сборка 22.08-487 · My Home — интерактивный прототип

Обзорная картинка — «схемы/схема-4-карта-данных.svg».

## Мир S — все коллекции
```js
const S = {
  buildings: {},     // id -> дом
  users: {},         // id -> пользователь
  payments: [],
  requests: [],
  votes: [],
  posts: [],
  works: [],
  meters: [],
  evPays: [],        // платежи за зарядку: жилец заявил -> председатель подтвердил
  expenses: [],
  bookings: [],
  tickets: [],
  hand: {},          // buildingId -> процедура смены председателя (раздел 3 ТЗ)
  /* ЛЕТОПИСЬ ЧЛЕНСТВА (Эдуард 09.08.2026, «сделать правильно»): кто, в какой
     квартире какого дома, в какой роли и с какого числа. Отдельная запись
     между человеком и домом — не поле на человеке и не список на доме.
     Зачем именно так: (1) правило «двое на квартиру» проверяется в ОДНОМ
     месте, а не в четырёх дверях по отдельности; (2) появляется история
     «кто жил здесь раньше» — без неё нельзя разобрать спор о долге квартиры;
     (3) отсоединение человека — честное закрытие записи (until), а не
     стирание поля, после которого не докажешь, что он тут вообще был.
     Поля u.buildingId и u.apt остаются рабочими для экранов, но пишутся
     ТОЛЬКО через memberOpen/memberClose. */
  /* ЧИТАЛКИ ИСТОРИИ ЗДЕСЬ НЕТ НАРОЧНО (чистка 09.08.2026): данные для разбора
     спора о долге лежат в этой летописи и достаются обычной выборкой по дому и
     квартире. Отдельную функцию я написал заранее, но ни один экран её так и не
     позвал — а мёртвый код в боевой части не должен ехать программисту. Когда
     будем делать экран спора, читалка пишется там же, одной строкой. */
  members: [],       // {id, buildingId, apt, userId, role, since, until, why}
  feed: [],          // лента активности платформы (для админки)
  /* промо-места (решение Эдуарда 01.08.2026): баннеры и бегущие строки на
     телефоне УПРАВЛЯЮТСЯ ИЗ АДМИНКИ, а не из кода. Контент платформенный —
     переживает пересев демо-мира. kind: card|ticker; tgt: all|nospec|spec;
     act: becomeSpec|spec|'' (куда ведёт тап). Тексты — контент, иврит. */
  promos: [
    /* ДЕМО: примеры, владелец платформы правит их в админке.
       ⚠ КАРТОЧКА «בעל מקצוע? קבלו הזמנות מהשכנים» УБРАНА 10.08.2026 по слову
       Эдуарда: «удали рекламу на иврите про специалиста, пусть останутся только
       три». Она дублировала партнёрскую витрину — та же мысль другими словами,
       и в общем круге получалось четыре предложения вместо трёх. Механика
       промо цела: владелец платформы заводит свои объявления в админке, и они
       встают в тот же круг. */
    { id:'pm2', on:false, kind:'ticker', title:'ההודעה הרצה שלכם כאן — נערך בפאנל הניהול',
      sub:'', btn:'', tgt:'all', act:'' },
  ],
  /* МАРКЕТПЛЕЙС ВЕЩЕЙ (чертёж маркетплейс-вещей.md, решения Эдуарда 08.08.2026).
     ПРАВИЛО (в бой): платформа НЕ сторона сделки — ни комиссий, ни чужих денег;
     доход только с необязательного продвижения. cfg — платформенный, правится
     в админке, переживает пересев демо-мира (как promos). */
  mkt: {
    cfg: {
      maxPhotos: 5,   /* ⚠ пять (15.08.2026, решение Эдуарда) */ lifeDays: 14, takeH: 0, flagN: 3,
            /* ⚠ ЦЕНЫ ПОДНЯТЫ (12.08.2026, решение Эдуарда «подними цены»).
         Было: поднятие объекта на 3 дня за ₪10. Для рынка, где сделка — десятки
         тысяч, такая цена читается как «мы не уверены в своей ценности»: за
         десять шекелей человек не ждёт результата и не расстроится, если его
         не будет. Ставим взрослые деньги и понятный срок.
         Подписка маклера — ₪249: он платит за ИНСТРУМЕНТ и объявления не
         ограничены; первые месяцы бесплатны, иначе он не увидит откликов и
         не поймёт, за что платит. */
      /* ⚠ ЦЕНА У КАЖДОГО ОХВАТА СВОЯ, А НЕ МНОЖИТЕЛЬ (15.08.2026, решение
       Эдуарда: «множители неправильные, я должен каждую назначать сам»).
       Он прав: множитель — удобство для нас, а не цена для человека. При ×3
       страна на 14 дней стоила бы 150; настоящая цена другая, и назначает её
       владелец.
       ТАБЛИЦА: город 10/15/20 · округ 15/20/25 · страна 20/30/40. В агоротах. */
    promo: [
      /* ⚠ ЦЕНЫ СТАЛИ ЧИСЛОМ (17.08.2026): охват перестал быть товаром — продаём
         только срок, и три цены по охватам стали не нужны. Объект в числовом
         поле давал бы «0[object Object]», как было у недвижимости. */
      { id:'up3',  days:3,  price:1000 },
      { id:'up7',  days:7,  price:1500 },
      { id:'up14', days:14, price:2000 },
    ],
    /* ⚠ ПЯТЬ ОБЪЯВЛЕНИЙ НА ЧЕЛОВЕКА (15.08.2026, решение Эдуарда), правится из
       админки. Без предела один продавец занял бы всю ленту. */
    maxItems: 5,
      areaMult: { city:1, reg:2, all:3 },   /* во сколько раз дороже округ и страна (12.08.2026) */
  /* ⚠ ЧЕТЫРЕ КАТЕГОРИИ ДОБАВЛЕНЫ (15.08.2026, решение Эдуарда). Они вытягивают
       из «Разного» то, что у соседей ходит чаще всего: спорт и велосипеды (их в
       доме много и они дорогие), книги и игры (часто даром), посуда (переезды и
       обновления), растения (отростки и горшки — живой обмен).
       БОЛЬШЕ НЕ ДЕЛАЕМ: полоса фильтров листается вбок, и при пятнадцати
       категориях человек не долистает половины. К тому же чем мельче деление,
       тем чаще промах: пылесос — техника или инструмент?
       Все они правятся из админки: имя, показ, добавление новых. */
    /* ⚠ КАТЕГОРИИ ПЕРЕСОБРАНЫ (17.08.2026, разбор с Эдуардом до конца).
       УБРАНЫ: «книги» и «растения» — их отдают редко, а место в списке занимают.
       «КУХНЯ» СЛИТА С «ДЛЯ ДОМА»: слово Эдуарда «кухня непонятно о чём».
       Посуда, свет, текстиль, декор — всё это одно дело: обстановка.
       ⚠ «ДАРОМ» УБРАНО ИЗ КАТЕГОРИЙ — это ЦЕНА, а не вид вещи. Диван даром это
       мебель. В списке оно путало: человек выбирал между «мебелью» и «даром»,
       хотя это разные вопросы. Фильтр «даром» остаётся и ищет по нулевой цене.
       ПОРЯДОК — ОТ ЧАСТОГО К РЕДКОМУ: мебель и техника ходовее всего на любой
       барахолке, «другое» всегда последним. */
    cats:  [ {id:'furn',on:true},{id:'tech',on:true},{id:'kids',on:true},
               {id:'home',on:true},{id:'cloth',on:true},{id:'tools',on:true},
               {id:'sport',on:true},{id:'other',on:true} ],
    /* ⚠ ПОДВИДЫ — СОБИРАЕМ СРАЗУ, ПОКАЗЫВАЕМ ПОТОМ (17.08.2026, находка Эдуарда:
       «но тогда это общая статистика, потом придётся по дроблению опять
       собирать»).
       ОН ПРАВ, И ЭТО БЫЛО БЫ БОЛЬНО: копить статистику по широкой «мебели»,
       а через год начать дробить — значит выбросить все накопленные месяцы.
       ПОЭТОМУ ДРОБИМ УЖЕ СЕЙЧАС, а на малой ленте просто не пользуемся: фильтры
       остаются по восьми категориям, лента по свежести. Подвид лежит записанным
       и ждёт своего часа.
       ПОДВИД НЕОБЯЗАТЕЛЕН: не выбрал — пишем «прочее». Иначе человек бросит
       форму на полпути. */
    /* ⚠ ПОРЯДОК ЛЕНТЫ — РУБИЛЬНИК (17.08.2026, решение Эдуарда).
       'fresh' — новое сверху: пока объявлений мало, это честнее и понятнее.
       'smart' — умный подбор: близость, цена против похожих, интерес, свежесть.
       Включать, когда наберётся хотя бы тысяча объявлений и пара сотен
       закрытых сделок — раньше данных просто не хватит. */
    order: 'fresh',
    /* ⚠ потолок платных в верхe ленты (22.08.2026, как у недвижимости) */
    promoTop: 3,
    subs: {
      furn:  ['sofa','table','closet','chair','bed','etc'],
      tech:  ['phone','comp','tv','home_app','etc'],
      kids:  ['stroller','carseat','crib','toys','etc'],
      home:  ['dishes','textile','light','decor','etc'],
      cloth: ['adult','child','shoes','etc'],
      /* ⚠ ИНСТРУМЕНТ — ПО ДЕЛУ, А НЕ ПО ЖЕЛЕЗКЕ (17.08.2026, слово Эдуарда:
         «нет, по-другому надо, умнее»). Человек берёт инструмент не «дрель»,
         а «повесить полку»: он не помнит, перфоратор у него или ударная дрель,
         но помнит, что сверлил стены. */
      tools: ['walls','wood','paint','garden','measure','etc'],
      sport: ['bike','trainer','gear','etc'],
      other: ['etc'],
    },
      /* контент — иврит (правило проекта): списки правит владелец в админке */
      banned: ['נשק ותחמושת','סמים ותרופות מרשם','בעלי חיים','טבק ואלכוהול לקטינים','מסמכים מזויפים ורכוש גנוב','שירותים אינטימיים'],
      stop:   ['אקדח','רובה','נשק','תחמושת','סמים','קנאביס','תרופת מרשם','גור','כלב למכירה','חתול למכירה','סיגריות','אלכוהול','תעודה מזויפת','גנוב'],
    },
    items: [], chats: [], subs: {},
  },
  /* =========================================================================
     МОДУЛЬ «НЕДВИЖИМОСТЬ / МАКЛЕР» (чертёж недвижимость-маклер.md, 08.08.2026).
     ПРАВИЛО (в бой): платформа НЕ сторона сделки и НЕ посредник — процента со
     сделки нет (חוק המתווכים!), денег клиента не касаемся. Доход: подписка
     маклера-ПАРТНЁРА + необязательное продвижение. Кешбэка/бонусов у маклеров
     НЕТ (программа специалистов); частник МОЖЕТ платить продвижение бонусами.
     Частнику бесплатно до cfg.freeCap объектов; ищущий не платит никогда.
     cfg — платформенный, правится в админке, переживает пересев демо-мира. */
  re: {
    cfg: {
      maxPhotos: 5,   /* ⚠ пять (15.08.2026, решение Эдуарда) */     /* ⚠ 90 ДНЕЙ ВМЕСТО 60 (20.08.2026, решение Эдуарда: «сделаем 90 дней, и у
       маклера, и у обычного человека»). Квартира продаётся дольше вещи: два
       месяца могло не хватить, и объявление снималось на середине разговора. */
    lifeDays: 90, flagN: 3, freeCap: 2, sub: 24900,
      /* по одному активному на вид сделки — см. reFreeLeft (12.08.2026) */
      capByKind: { rent: 0, sale: 0 },   /* 0 = без потолка (решение Эдуарда 12.08.2026) */
      /* ⚠ ПОДПИСКА МАКЛЕРА ₪249 (12.08.2026, решение Эдуарда: «с маклеров брать
         только за подписку, вначале бесплатно; подними цены»).
         Было ₪150 и потолок объявлений. Потолок для маклера убран совсем: он
         платит за ИНСТРУМЕНТ, а не за штуки, и упереться в стену после оплаты —
         худшее, что можно сделать с профессионалом.
         Бесплатные первые месяцы обязательны: маклер не заплатит за пустую
         площадку, где нет ни просмотров, ни звонков. Поэтому всё это время ему
         НАДО СЧИТАТЬ И ПОКАЗЫВАТЬ пользу — просмотры и отклики; иначе к моменту
         счёта ему нечего будет вспомнить, и он просто не продлит. */
      /* ⚠ ЦЕНА В АГОРОТ, КАК ВЕЗДЕ (15.08.2026, находка при подстановке цен в
       условия): стояло 249 — и на экране выходило «₪2», потому что все деньги в
       приложении хранятся в агорот. Подписка специалиста рядом записана как
       9000, то есть ₪90. Стало 24900 = ₪249. */
    sub: 24900, trialM: 3,
            /* ⚠ ЦЕНЫ ПОДНЯТЫ (12.08.2026, решение Эдуарда «подними цены»).
         Было: поднятие объекта на 3 дня за ₪10. Для рынка, где сделка — десятки
         тысяч, такая цена читается как «мы не уверены в своей ценности»: за
         десять шекелей человек не ждёт результата и не расстроится, если его
         не будет. Ставим взрослые деньги и понятный срок.
         Подписка маклера — ₪249: он платит за ИНСТРУМЕНТ и объявления не
         ограничены; первые месяцы бесплатны, иначе он не увидит откликов и
         не поймёт, за что платит. */
      /* ⚠ ЦЕНА У КАЖДОГО ОХВАТА СВОЯ, А НЕ МНОЖИТЕЛЬ (15.08.2026, решение
       Эдуарда: «множители неправильные, я должен каждую назначать сам»).
       Он прав: множитель — удобство для нас, а не цена для человека. При ×3
       страна на 14 дней стоила бы 150; настоящая цена другая, и назначает её
       владелец.
       ТАБЛИЦА: город 10/15/20 · округ 15/20/25 · страна 20/30/40. В агоротах. */
      promo: [
        /* ⚠ СРОКИ И ЦЕНЫ ПОД ЖИЛЬЁ (15.08.2026, слово Эдуарда: «время продвижения
           и цены только надо изменить 14, 21, 31 — я думаю, просто сколько дней
           нужно для эффекта реального»).
           ОН ПРАВ: три дня для квартиры — деньги на ветер. Жильё ищут неделями:
           человек смотрит, думает, советуется с семьёй, ездит глядеть. Барахолке
           три дня в самый раз — диван увозят за час, — а тут нет.
           31 взял 30: месяц ровный, привычнее.
           ЦЕНЫ ПОСТРОЕНЫ ТАК, ЧТОБЫ ДЛИННОЕ БЫЛО ВЫГОДНЕЕ: месяц за ₪49 дешевле
           двух двухнедельных. Ему спокойнее, нам тоже.
           ЭДУАРД ОТМЕТИЛ: за квартиру платят иначе, чем за диван, и цены ещё
           поднимутся — правится одним числом в админке. */
        { id:'up14', days:14, price:{ city:2900, reg:2900, all:2900 } },
        { id:'up21', days:21, price:{ city:3900, reg:3900, all:3900 } },
        { id:'up30', days:30, price:{ city:4900, reg:4900, all:4900 } },
      ],
    /* ⚠ ПЯТЬ ОБЪЯВЛЕНИЙ НА ЧЕЛОВЕКА (15.08.2026, решение Эдуарда), правится из
       админки. Без предела один продавец занял бы всю ленту. */
      maxItems: 5,
      /* ⚠ сколько объектов частнику даром (15.08.2026, решение Эдуарда): один.
         Своя квартира — не дело; несколько — уже дело, и за них платят. */
      /* ⚠ сколько платных пускаем в первую десятку (15.08.2026, аудит):
         три. Больше — и лента становится рекламной доской. */
      promoTop: 3,
      /* ⚠ через сколько дней заморозки — сигнал о блокировке маклера
         (17.08.2026, как у специалиста) */
      blockDays: 30,
      freeItems: 1,
      /* ⚠ ₪150 ЗА ВТОРОЕ МЕСТО В ТОМ ЖЕ СОЧЕТАНИИ (20.08.2026, решение Эдуарда).
       Было ₪29 — цена вещи с барахолки, а не квартиры. Объявление о квартире
       стоит иначе и живёт дольше. */
    itemPrice: 15000,   /* ₪29 в месяц за каждый объект сверх первого */
      areaMult: { city:1, reg:2, all:3 },   /* во сколько раз дороже округ и страна (12.08.2026) */
      stop: ['נשק','סמים','מזויף','גנוב'],
    },
    items: [], partners: [], chats: [],
  },
  /* ТЕХНИК ТЕЛЕФОНОВ — третья шкура партнёра (решение Эдуарда 09.08.2026).
     МОДУЛЬ ЕЩЁ НЕ ГОТОВ: есть иконка и городской сервис, кабинета нет.
     Но РОЛЬ и МОДУЛЬ — разные вещи, и роль заводим сразу. Почему не потом:
     если третью шкуру завести только вместе с кабинетом, весь код до тех пор
     будет знать ровно про две, и каждое место, где шкуры перечисляются
     (панель персонажей, переключатель режимов, выбор отдела, «стать
     партнёром»), придётся искать заново и править поодиночке. Реестр из трёх
     с самого начала означает, что четвёртая шкура потом — одна строка. */
  tech: { partners: [] },
  /* =========================================================================
     МОДУЛЬ «МОЙ ГОРОД» (МОДУЛИ.md §4). Данные города ОТКРЫТЫЕ — партнёр не
     нужен. srcs: источники подкачки (в бою — RSS/JSON мэрии, в демо — снимок).
     asks: спрос на города, которых ещё нет — очередь на подключение. */
  city: {
    cfg: {
      pullMin: 30, keepDays: 45,
      rubs: [ {id:'alert',on:true}, {id:'news',on:true}, {id:'work',on:true},
              {id:'cult',on:true},  {id:'sport',on:true}, {id:'kids',on:true} ],
    },
    srcs: [], items: [], pull: {}, read: {}, rem: {}, asks: {},
  },
  /* =========================================================================
     МОДУЛЬ «ТРАНСПОРТ» (решения Эдуарда 06.08.2026). НАСТОЯЩИЙ модуль, не витрина.
     Источник — открытые данные Минтранса Израиля (GTFS: маршруты, остановки,
     расписания на 60 дней + живой поток GTFS-realtime «где автобус сейчас»).
     Автобусы И поезда в одном потоке. На старте показываем ближайшие остановки
     к дому и ближайшие рейсы; карта с живым движением — потом (украшение).
     ДЕМО: посев transitSeed() помечен demo:true. БОЙ: подкачка по дому/гео.
     ========================================================================= */
  transit: {
    cfg: { pullMin: 1, radiusM: 600, live: false },   /* live=GTFS-realtime, пока выкл */
    stops: [], lines: [], deps: [], asks: {},
  },
  /* =========================================================================
     МОДУЛЬ «ПОДРАБОТКА ПОДРОСТКОВ» (решения Эдуарда 08.08.2026).
     Пользуются ВЗРОСЛЫЕ: родитель ищет подработку ребёнку или ищет помощника.
     Одна лента, две метки; категорий нет — свободный текст.
     Прямой контакт закрыт по умолчанию; открывается галочкой взрослого. */
  job: {
    cfg: {
      /* ⚠ ЧЕТЫРНАДЦАТЬ ДНЕЙ (15.08.2026, решение Эдуарда): «срок жизни нужен для
       правильного объявления, чтоб всем досталось». Потом можно обновить. */
    lifeDays: 14, flagN: 3,
      /* ⚠ ДВА ОБЪЯВЛЕНИЯ С КВАРТИРЫ (20.08.2026, решение Эдуарда). По квартире, а
         не по человеку: в одной может быть привязано двое, и предел на человека
         дал бы им вдвое больше места, чем соседу-одиночке. */
      maxPerApt: 2,
      /* контент — иврит: взрослая работа и всё, чему рядом с подростком не место */
      stop: ['אלכוהול','סיגריות','הימורים','נשק','סמים','משמרת לילה','נהיגה'],
    },
    items: [], chats: [],
  },
  /* партнёрские витрины-заглушки: спрос копится до разговора с партнёром */
  stubs: { asks: {} },
  seq: 1,
};
```

## Модуль специалистов (рычаги живые, из админки)
```js
function specEmpty(){ return {
  /* ДЕМО: в жизни оба рубильника выключены при запуске (ТЗ-03, раздел 0.1).
     enabled здесь true только затем, чтобы модуль было видно на показе. */
  enabled: true,
  /* «рубильника денег» больше нет (отмена Эдуарда 02.08.2026): монетизация
     живёт всегда, бесплатность — это ДЛИНА бесплатного периода (freeM
     глобально + подарочные месяцы точечно из админки). */
    /* ⚠ КОМИССИЯ СО СДЕЛОК ОТКЛЮЧЕНА (20.08.2026, решение Эдуарда).
       ⚠ ПОЧЕМУ. Мы брали 8% с работы, которой НЕ ВИДИМ: сделка идёт между двумя
       людьми мимо нас, и узнать о ней можно было только СПРОСИВ обоих. Отсюда
       вырастало дерево из семи веток: весы сумм, допуск расхождения, три
       напоминания, засчёт по слову одного, метки за занижение, споры, сигналы
       оператору. А в самом низу — просьба к соседям свидетельствовать друг на
       друга: жилец называет сумму, мы идём с ней к мастеру, и он поймёт, кто
       сказал. Это не Яд2, где люди чужие: тут сосед и мастер живут в одном доме.
       ⚠ СТАЛО: ПОДПИСКА И ТОЛЬКО. Специалист платит ₪129 в месяц и получает
       доступ — что он заработал, нас не касается. И бухгалтерии легче: один счёт
       в месяц вместо чека на каждую сделку.
       ⚠ ЧИСТКА ИДЁТ В ТРИ ЗАХОДА, И ЭТО НАРОЧНО: комиссия вросла в 185 мест —
       сделки, весы, споры, чат специалиста. Резать всё разом значит сломать
       много. СЕГОДНЯ ПЕРВЫЙ: комиссия не берётся, сделки уходят с глаз, письма
       молчат. Код остаётся спящим — его вычистим вторым заходом.
       fee:0 — единый выключатель: всё, что считало комиссию, даёт ноль. */
    thr:3, tol:10, fee:0, split:[0,0,0], sub:12900, freeM:3,
    /* ⚠ условия по ремеслу (20.08.2026): пусто — по общему */
    subCat:{}, freeCat:{}, autoDays:10, burnM:12,
    dealsOff:true,   /* ⚠ сделок нет: экраны и письма молчат */
  /* приёмка 02.08.2026: thr — это ПАРЫ «вопрос-ответ» (не сообщения);
     feeCat — комиссия по профессиям (cat → %), feeInd — по специалисту
     (id → %); приоритет: специалист → профессия → глобальная fee.
     favs — «Мои специалисты» жильцов (uId → [masterId]). Всё меняется
     из админки в любой момент — проценты живые, не константы. */
  feeCat:{}, feeInd:{}, favs:{},
  /* маркетплейс (решение Эдуарда 02.08.2026): пояс подаёт платформа.
     promoMax — сколько платных мест наверху пояса (защита от засилья рекламы),
     promoP   — цена продвижения, агороты/мес (рычаг админки) */
    /* ⚠ ТРИ СРОКА ВМЕСТО ЧЕТЫРЁХ УСЛУГ (17.08.2026, решение Эдуарда — как в
       недвижимости). Цены ниже квартирных: заказ электрика дешевле сделки с
       жильём, и платить за подъём столько же он не станет. */
    promo: [
      { id:'sp14', days:14, price:1900 },
      { id:'sp21', days:21, price:2600 },
      { id:'sp30', days:30, price:3300 },
    ],
    /* ⚠ сроки долга — в админке (17.08.2026, слово Эдуарда «сделай, чтоб меняли
       этот срок в админке»): через сколько дней заморозка и через сколько после
       неё — сигнал о блокировке. */
    freezeDays: 14,
    blockDays: 30,
  promoMax:2, promoP:4900,
  /* цены услуг за бонусы, агороты (приёмка 02.08.2026) — рычаги админки */
  svc:{ up:1500, badge:2500, port:1000, day:4000 }, svcM:1, svcPh:3, phFree:5, profReqs:[],
  masters:{}, chats:{}, deals:[], reviews:[], pts:{}
}
```

## Биллинг дома
`S.billing = S.billing || { on:false, graceDays:14,   /* ⚠ дней отсрочки до режима чтения (15.08.2026) */ thr:8, low:12900, high:17900,` — агороты; thr — порог этажей.